#ifndef GLOBALS_H
#define GLOBALS_H

// Forward-declare to avoid header cycles
class AnimatedAsset;

// Global variables (addresses included in symbol names to reflect original binary offsets)
extern AnimatedAsset* g_TextManager_00436990;

#endif // GLOBALS_H
